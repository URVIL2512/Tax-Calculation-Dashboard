services:
  - type: web
    name: tax-calculation-dashboard
    env: static
    buildCommand: cd tax_mui/tax_mui && npm install && npm run build
    staticPublishPath: ./tax_mui/tax_mui/build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html